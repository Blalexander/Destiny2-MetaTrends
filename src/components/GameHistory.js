import React from 'react'

export default function GameHistory(props) {
  // const accountsName = props[0][0].displayName;

  return (
    <div id="gameHistoryContainer" value={props}>
      <div id="accountName">heyo</div>
    </div>
  )
}